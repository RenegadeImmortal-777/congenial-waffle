{
  "name": "medpass",
  "version": "1.0.0",
  "private": true,
  "description": "MedPass — AI tutor app with email/password auth (verification + reset), built on Node built-ins only (http, node:sqlite, node:crypto). No external dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  }
}
